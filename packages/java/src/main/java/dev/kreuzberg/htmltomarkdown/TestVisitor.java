// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:79e71c5a5e14929db6dffa64695f3efdcafa5d99f3603134bc2a6d208a358dfc
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.htmltomarkdown;

/** Test-friendly visitor interface using VisitContext instead of NodeContext. */
public interface TestVisitor {
    /** Called for text nodes. */
    default VisitResult visitText(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called before entering any element. */
    default VisitResult visitElementStart(final VisitContext ctx) {
        return VisitResult.continueDefault();
    }
    /** Called after exiting any element; receives the default markdown output. */
    default VisitResult visitElementEnd(final VisitContext ctx, final String output) {
        return VisitResult.continueDefault();
    }
    /** Called for anchor links. title is null when the attribute is absent. */
    default VisitResult visitLink(final VisitContext ctx, final String href, final String text, final String title) {
        return VisitResult.continueDefault();
    }
    /** Called for images. title is null when absent. */
    default VisitResult visitImage(final VisitContext ctx, final String src, final String alt, final String title) {
        return VisitResult.continueDefault();
    }
    /** Called for heading elements h1-h6. id is null when absent. */
    default VisitResult visitHeading(final VisitContext ctx, final int level, final String text, final String id) {
        return VisitResult.continueDefault();
    }
    /** Called for code blocks. lang is null when absent. */
    default VisitResult visitCodeBlock(final VisitContext ctx, final String lang, final String code) {
        return VisitResult.continueDefault();
    }
    /** Called for inline code elements. */
    default VisitResult visitCodeInline(final VisitContext ctx, final String code) {
        return VisitResult.continueDefault();
    }
    /** Called for list items. */
    default VisitResult visitListItem(final VisitContext ctx, final boolean ordered, final String marker,
            final String text) {
        return VisitResult.continueDefault();
    }
    /** Called before processing a list. */
    default VisitResult visitListStart(final VisitContext ctx, final boolean ordered) {
        return VisitResult.continueDefault();
    }
    /** Called after processing a list. */
    default VisitResult visitListEnd(final VisitContext ctx, final boolean ordered, final String output) {
        return VisitResult.continueDefault();
    }
    /** Called before processing a table. */
    default VisitResult visitTableStart(final VisitContext ctx) {
        return VisitResult.continueDefault();
    }
    /** Called for table rows. cells contains the cell text values. */
    default VisitResult visitTableRow(final VisitContext ctx, final java.util.List<String> cells,
            final boolean isHeader) {
        return VisitResult.continueDefault();
    }
    /** Called after processing a table. */
    default VisitResult visitTableEnd(final VisitContext ctx, final String output) {
        return VisitResult.continueDefault();
    }
    /** Called for blockquote elements. */
    default VisitResult visitBlockquote(final VisitContext ctx, final String content, final long depth) {
        return VisitResult.continueDefault();
    }
    /** Called for strong/bold elements. */
    default VisitResult visitStrong(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for emphasis/italic elements. */
    default VisitResult visitEmphasis(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for strikethrough elements. */
    default VisitResult visitStrikethrough(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for underline elements. */
    default VisitResult visitUnderline(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for subscript elements. */
    default VisitResult visitSubscript(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for superscript elements. */
    default VisitResult visitSuperscript(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for mark/highlight elements. */
    default VisitResult visitMark(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for line break elements. */
    default VisitResult visitLineBreak(final VisitContext ctx) {
        return VisitResult.continueDefault();
    }
    /** Called for horizontal rule elements. */
    default VisitResult visitHorizontalRule(final VisitContext ctx) {
        return VisitResult.continueDefault();
    }
    /** Called for custom or unknown elements. */
    default VisitResult visitCustomElement(final VisitContext ctx, final String tagName, final String html) {
        return VisitResult.continueDefault();
    }
    /** Called before a definition list. */
    default VisitResult visitDefinitionListStart(final VisitContext ctx) {
        return VisitResult.continueDefault();
    }
    /** Called for definition term elements. */
    default VisitResult visitDefinitionTerm(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for definition description elements. */
    default VisitResult visitDefinitionDescription(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called after a definition list. */
    default VisitResult visitDefinitionListEnd(final VisitContext ctx, final String output) {
        return VisitResult.continueDefault();
    }
    /** Called for form elements. action and method may be null. */
    default VisitResult visitForm(final VisitContext ctx, final String action, final String method) {
        return VisitResult.continueDefault();
    }
    /** Called for input elements. name and value may be null. */
    default VisitResult visitInput(final VisitContext ctx, final String inputType, final String name,
            final String value) {
        return VisitResult.continueDefault();
    }
    /** Called for button elements. */
    default VisitResult visitButton(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called for audio elements. src may be null. */
    default VisitResult visitAudio(final VisitContext ctx, final String src) {
        return VisitResult.continueDefault();
    }
    /** Called for video elements. src may be null. */
    default VisitResult visitVideo(final VisitContext ctx, final String src) {
        return VisitResult.continueDefault();
    }
    /** Called for iframe elements. src may be null. */
    default VisitResult visitIframe(final VisitContext ctx, final String src) {
        return VisitResult.continueDefault();
    }
    /** Called for details elements. */
    default VisitResult visitDetails(final VisitContext ctx, final boolean open) {
        return VisitResult.continueDefault();
    }
    /** Called for summary elements. */
    default VisitResult visitSummary(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called before a figure element. */
    default VisitResult visitFigureStart(final VisitContext ctx) {
        return VisitResult.continueDefault();
    }
    /** Called for figcaption elements. */
    default VisitResult visitFigcaption(final VisitContext ctx, final String text) {
        return VisitResult.continueDefault();
    }
    /** Called after a figure element. */
    default VisitResult visitFigureEnd(final VisitContext ctx, final String output) {
        return VisitResult.continueDefault();
    }
}
