// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bcfecf9e035b6ee869397939083bb35029cf2ae71068f1e3c7736324ee4c4644
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.htmltomarkdown;

import java.lang.foreign.MemorySegment;

/**
 * Type alias for a visitor handle ({@code Arc}-wrapped {@code Mutex} for thread-safe shared mutation).
 *
 * {@code Send + Sync} so that types embedding a {@code VisitorHandle} (e.g. {@code ConversionOptions})
 * can be shared across threads — required by callers that stash configs inside
 * axum/rmcp/tokio Send-bound contexts.
 */
public class VisitorHandle implements AutoCloseable {
    private final MemorySegment handle;

    VisitorHandle(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.HTM_VISITOR_HANDLE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free VisitorHandle: " + e.getMessage(), e);
            }
        }
    }
}
