// Generated by alef. Do not edit by hand.

plugins {
    id("com.android.library") version "9.0.0"
    kotlin("android") version "2.3.21"
}

android {
    namespace = "dev.kreuzberg.android"
    compileSdk = 36

    defaultConfig {
        minSdk = 21
        consumerProguardFiles("consumer-rules.pro")
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    sourceSets {
        getByName("main") {
            jniLibs.srcDirs("src/main/jniLibs")
        }
    }
}

dependencies {
    implementation("org.jetbrains.kotlin:kotlin-stdlib")
    testImplementation("junit:junit:4.13.2")
    androidTestImplementation("androidx.test.ext:junit:1.3.0")
    androidTestImplementation("androidx.test.espresso:espresso-core:3.7.0")
}

// Copy the Rust shared library into the JNI libs directory.
// Run: cargo build --target aarch64-linux-android --release
// then copy target/aarch64-linux-android/release/libhtml_to_markdown_ffi.so
// into src/main/jniLibs/arm64-v8a/ before building.
