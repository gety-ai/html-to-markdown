// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9fe00062bdf9e822b23c3acd61316f9466415d025b7e57f016c9c9a5463e117f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import HtmlToMarkdownRs
import RustBridge

/// E2e tests for category: conversion.
final class ConversionTests: XCTestCase {
    func testBlockquoteMultipleParagraphs() throws {
        // Blockquote with multiple paragraphs has each paragraph prefixed
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: blockquote_multiple_paragraphs)");
    }

    func testBlockquoteNested() throws {
        // Nested blockquote produces double-prefixed lines
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: blockquote_nested)");
    }

    func testBlockquoteSimple() throws {
        // Simple blockquote
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: blockquote_simple)");
    }

    func testBlockquoteWithList() throws {
        // Blockquote containing a list preserves list items inside quote
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: blockquote_with_list)");
    }

    func testBoldAndItalic() throws {
        // Nested bold and italic
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: bold_and_italic)");
    }

    func testBoldStrong() throws {
        // Strong tag converts to bold
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: bold_strong)");
    }

    func testCodeBlock() throws {
        // Code block with language preserves content
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: code_block)");
    }

    func testCodeBlockNoLanguage() throws {
        // Code block without a language class preserves content
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: code_block_no_language)");
    }

    func testCodeInlineInParagraph() throws {
        // Inline code element nested inside a paragraph
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: code_inline_in_paragraph)");
    }

    func testCodeWithBackticksInContent() throws {
        // Inline code containing backtick characters is properly escaped
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: code_with_backticks_in_content)");
    }

    func testEmphasisMarkHighlight() throws {
        // mark tag produces highlighted output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: emphasis_mark_highlight)");
    }

    func testEmphasisStrikethroughDel() throws {
        // del tag converts to GFM strikethrough
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: emphasis_strikethrough_del)");
    }

    func testEmphasisStrikethroughS() throws {
        // s tag converts to GFM strikethrough
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: emphasis_strikethrough_s)");
    }

    func testEmphasisSubscript() throws {
        // sub tag content is preserved
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: emphasis_subscript)");
    }

    func testEmphasisSuperscript() throws {
        // sup tag content is preserved
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: emphasis_superscript)");
    }

    func testEmphasisUnderlineU() throws {
        // u tag content is preserved in output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: emphasis_underline_u)");
    }

    func testFormInputElements() throws {
        // Form input elements produce readable output without form mechanics
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: form_input_elements)");
    }

    func testFormSelectOptions() throws {
        // Select element with options produces readable output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: form_select_options)");
    }

    func testFormTextarea() throws {
        // Textarea element produces readable output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: form_textarea)");
    }

    func testHeadingH1() throws {
        // H1 heading
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: heading_h1)");
    }

    func testHeadingH2() throws {
        // H2 heading
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: heading_h2)");
    }

    func testHeadingH3() throws {
        // H3 heading
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: heading_h3)");
    }

    func testHeadingH4() throws {
        // H4 heading
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: heading_h4)");
    }

    func testHeadingH5() throws {
        // H5 heading
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: heading_h5)");
    }

    func testHeadingH6() throws {
        // H6 heading
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: heading_h6)");
    }

    func testImageFigureFigcaption() throws {
        // Figure with figcaption preserves both image and caption
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: image_figure_figcaption)");
    }

    func testImageLinked() throws {
        // Image inside an anchor produces a linked image
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: image_linked)");
    }

    func testImageNoAlt() throws {
        // Image without alt text produces image markdown
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: image_no_alt)");
    }

    func testImageSimple() throws {
        // Image with alt text
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: image_simple)");
    }

    func testImageWithTitle() throws {
        // Image with title attribute includes title in output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: image_with_title)");
    }

    func testInlineCode() throws {
        // Inline code
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: inline_code)");
    }

    func testItalicEm() throws {
        // Em tag converts to italic
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: italic_em)");
    }

    func testLineBreakBrTag() throws {
        // Single br tag produces a line break in output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: line_break_br_tag)");
    }

    func testLineBreakHrTag() throws {
        // hr tag produces a horizontal separator between content
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: line_break_hr_tag)");
    }

    func testLineBreakMultipleBr() throws {
        // Multiple consecutive br tags in sequence
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: line_break_multiple_br)");
    }

    func testLinkAnchorFragment() throws {
        // Fragment-only anchor link is preserved
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: link_anchor_fragment)");
    }

    func testLinkEmptyHref() throws {
        // Link with empty href produces output with the link text
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: link_empty_href)");
    }

    func testLinkImageInside() throws {
        // Image inside a link produces a linked image
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: link_image_inside)");
    }

    func testLinkMailto() throws {
        // Mailto link is preserved with mailto: scheme
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: link_mailto)");
    }

    func testLinkSimple() throws {
        // Simple link
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: link_simple)");
    }

    func testLinkWithBoldText() throws {
        // Link containing bold text preserves formatting
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: link_with_bold_text)");
    }

    func testLinkWithTitle() throws {
        // Link with title attribute
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: link_with_title)");
    }

    func testListDefinitionDl() throws {
        // Definition list with dt and dd elements
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: list_definition_dl)");
    }

    func testListItemMultipleParagraphs() throws {
        // List item containing multiple paragraphs
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: list_item_multiple_paragraphs)");
    }

    func testListMixedNested() throws {
        // Mixed list: ordered list nested inside unordered list
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: list_mixed_nested)");
    }

    func testListNestedOrdered() throws {
        // Nested ordered list with two levels of depth
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: list_nested_ordered)");
    }

    func testListNestedUnordered() throws {
        // Nested unordered list with two levels of depth
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: list_nested_unordered)");
    }

    func testListTaskCheckboxes() throws {
        // Task list with checked and unchecked checkboxes
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: list_task_checkboxes)");
    }

    func testOrderedList() throws {
        // Ordered list
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: ordered_list)");
    }

    func testParagraphMultiple() throws {
        // Multiple paragraphs are separated by a blank line
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: paragraph_multiple)");
    }

    func testParagraphNestedDivs() throws {
        // Text nested inside divs is extracted correctly
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: paragraph_nested_divs)");
    }

    func testParagraphSimple() throws {
        // Simple paragraph converts to plain text
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: paragraph_simple)");
    }

    func testParagraphWithInlineFormatting() throws {
        // Paragraph with bold, italic, and a link
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: paragraph_with_inline_formatting)");
    }

    func testParagraphWithLineBreaks() throws {
        // Paragraph with br tags produces line breaks in output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: paragraph_with_line_breaks)");
    }

    func testSemanticAbbr() throws {
        // Abbreviation element text is preserved
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: semantic_abbr)");
    }

    func testSemanticArticle() throws {
        // Article element wrapping content preserves inner content
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: semantic_article)");
    }

    func testSemanticDefinitionList() throws {
        // Definition list with term and description
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: semantic_definition_list)");
    }

    func testSemanticDetailsSummary() throws {
        // Details and summary elements produce readable output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: semantic_details_summary)");
    }

    func testSemanticHr() throws {
        // Horizontal rule produces a separator in output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: semantic_hr)");
    }

    func testSemanticMarkHighlight() throws {
        // Mark tag produces highlighted output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: semantic_mark_highlight)");
    }

    func testSemanticSectionWithHeading() throws {
        // Section element with heading preserves structure
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: semantic_section_with_heading)");
    }

    func testSemanticSubSuperscript() throws {
        // Subscript and superscript elements are preserved in output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: semantic_sub_superscript)");
    }

    func testSimpleTable() throws {
        // Simple table with header
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: simple_table)");
    }

    func testTableEmpty() throws {
        // Empty table produces no output or minimal output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: table_empty)");
    }

    func testTableNoThead() throws {
        // Table without thead uses first row as implied header
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: table_no_thead)");
    }

    func testTablePipeCharsInContent() throws {
        // Table cells containing pipe characters are escaped in output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: table_pipe_chars_in_content)");
    }

    func testTableWithAlignment() throws {
        // Table with column alignment attributes
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: table_with_alignment)");
    }

    func testTableWithColspan() throws {
        // Table with colspan attribute in a header cell
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: table_with_colspan)");
    }

    func testUnorderedList() throws {
        // Unordered list
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: unordered_list)");
    }

}
