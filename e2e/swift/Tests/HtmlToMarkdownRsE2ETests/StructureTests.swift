// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ffadb485b0ff5dd0929919b13c577c71383075d10b91aad4d10e72c72a21dbc1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import HtmlToMarkdownRs
import RustBridge

/// E2e tests for category: structure.
final class StructureTests: XCTestCase {
    func testStructureCodeBlock() throws {
        // Fenced code block produces Code node
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: structure_code_block)");
    }

    func testStructureDeepNestingH1H2H3() throws {
        // H1 > H2 > H3 creates three levels of heading nesting
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: structure_deep_nesting_h1_h2_h3)");
    }

    func testStructureH1H2NestedGroup() throws {
        // H1 followed by H2 creates a nested group under the H1
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: structure_h1_h2_nested_group)");
    }

    func testStructureHeadingParagraph() throws {
        // Simple heading followed by paragraph produces Heading and Paragraph nodes
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: structure_heading_paragraph)");
    }

    func testStructureList() throws {
        // Unordered list produces List and ListItem nodes
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: structure_list)");
    }

    func testStructureMultipleHeadings() throws {
        // Multiple headings create multiple Heading nodes with correct levels
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: structure_multiple_headings)");
    }

    func testStructureSiblingH1Groups() throws {
        // H1, H2, then another H1 creates two sibling top-level groups
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: structure_sibling_h1_groups)");
    }

}
