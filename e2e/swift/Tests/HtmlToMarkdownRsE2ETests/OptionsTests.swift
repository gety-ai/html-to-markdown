// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:528c789e3088a2b91559d1e21e5e09de4fcc5516312bd7560a1ca24cbea49d3f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import HtmlToMarkdownRs
import RustBridge

/// E2e tests for category: options.
final class OptionsTests: XCTestCase {
    func testOptionsAutolinksFalse() throws {
        // Bare URL links rendered as regular markdown links when autolinks disabled
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_autolinks_false)");
    }

    func testOptionsBrInTablesFalse() throws {
        // BR elements in table cells are stripped when disabled
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_br_in_tables_false)");
    }

    func testOptionsBrInTablesTrue() throws {
        // BR elements in table cells render as line breaks
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_br_in_tables_true)");
    }

    func testOptionsCodeBlockBackticks() throws {
        // Backticks code block style uses triple backtick fences
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_code_block_backticks)");
    }

    func testOptionsCodeBlockIndented() throws {
        // Code blocks use 4-space indentation
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_code_block_indented)");
    }

    func testOptionsCodeBlockTildes() throws {
        // Code blocks use tilde fences
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_code_block_tildes)");
    }

    func testOptionsCodeBlockTildesStyle() throws {
        // Tildes code block style uses triple tilde fences
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_code_block_tildes_style)");
    }

    func testOptionsCodeLanguagePython() throws {
        // Default code language annotation on blocks without lang attribute
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_code_language_python)");
    }

    func testOptionsConvertAsInline() throws {
        // Block elements treated as inline
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_convert_as_inline)");
    }

    func testOptionsDebugTrue() throws {
        // Debug mode enabled does not crash and produces output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_debug_true)");
    }

    func testOptionsDefaultTitleTrue() throws {
        // Links without title get empty title attribute when defaultTitle is true
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_default_title_true)");
    }

    func testOptionsEncodingUtf8() throws {
        // UTF-8 encoding hint for special characters
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_encoding_utf8)");
    }

    func testOptionsEscapeAsciiEnabled() throws {
        // ASCII Markdown characters are escaped when escapeAscii is true
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_escape_ascii_enabled)");
    }

    func testOptionsEscapeAsterisks() throws {
        // escape_asterisks option escapes asterisks in plain text
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_escape_asterisks)");
    }

    func testOptionsEscapeMisc() throws {
        // escape_misc option escapes miscellaneous markdown characters
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_escape_misc)");
    }

    func testOptionsEscapeUnderscores() throws {
        // escape_underscores option escapes underscores in plain text
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_escape_underscores)");
    }

    func testOptionsExcludeSelectorsAttribute() throws {
        // Elements matching CSS attribute selector are excluded entirely
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_exclude_selectors_attribute)");
    }

    func testOptionsExcludeSelectorsClass() throws {
        // Elements matching CSS class selector are excluded entirely
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_exclude_selectors_class)");
    }

    func testOptionsExcludeSelectorsEmptyNoop() throws {
        // Empty exclude_selectors list does not affect output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_exclude_selectors_empty_noop)");
    }

    func testOptionsExcludeSelectorsId() throws {
        // Elements matching CSS id selector are excluded entirely
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_exclude_selectors_id)");
    }

    func testOptionsExcludeSelectorsMultiple() throws {
        // Multiple CSS selectors each exclude their matched elements
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_exclude_selectors_multiple)");
    }

    func testOptionsExcludeSelectorsNestedContentDropped() throws {
        // All descendants of excluded elements are dropped
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_exclude_selectors_nested_content_dropped)");
    }

    func testOptionsExcludeSelectorsPlainTextMode() throws {
        // Exclude selectors work in plain text output mode
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_exclude_selectors_plain_text_mode)");
    }

    func testOptionsExcludeSelectorsVsStripTags() throws {
        // exclude_selectors drops entire subtree unlike strip_tags which keeps children
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_exclude_selectors_vs_strip_tags)");
    }

    func testOptionsExtractMetadataTrue() throws {
        // Extract metadata returns document metadata when enabled
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_extract_metadata_true)");
    }

    func testOptionsHeadingStyleAtx() throws {
        // ATX heading style produces hash-prefixed headings
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_heading_style_atx)");
    }

    func testOptionsHeadingStyleAtxClosed() throws {
        // ATX closed heading style adds closing hashes
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_heading_style_atx_closed)");
    }

    func testOptionsHeadingStyleUnderlined() throws {
        // Underlined heading style produces setext-style headings for h1 and h2
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_heading_style_underlined)");
    }

    func testOptionsHighlightBold() throws {
        // Mark tag rendered as bold
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_highlight_bold)");
    }

    func testOptionsHighlightDoubleEqual() throws {
        // Mark tag with double equal highlight style
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_highlight_double_equal)");
    }

    func testOptionsHighlightNone() throws {
        // Mark tag with no highlight style strips the mark
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_highlight_none)");
    }

    func testOptionsKeepInlineImagesInParagraph() throws {
        // Images inside specified tags stay inline
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_keep_inline_images_in_paragraph)");
    }

    func testOptionsLinkStyleReference() throws {
        // Links use reference-style formatting
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_link_style_reference)");
    }

    func testOptionsListCustomBullets() throws {
        // Custom bullet character for unordered lists
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_list_custom_bullets)");
    }

    func testOptionsListIndentTabs() throws {
        // Tab indentation type for nested list items
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_list_indent_tabs)");
    }

    func testOptionsListIndentWidthFour() throws {
        // Nested lists indented with 4 spaces per level
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_list_indent_width_four)");
    }

    func testOptionsMaxDepthDefaultUnlimited() throws {
        // Default max_depth (null) converts deeply nested content fully
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_max_depth_default_unlimited)");
    }

    func testOptionsMaxDepthTruncates() throws {
        // max_depth truncates content beyond the specified depth
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_max_depth_truncates)");
    }

    func testOptionsMaxDepthZeroEmpty() throws {
        // max_depth of 0 produces empty output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_max_depth_zero_empty)");
    }

    func testOptionsNewlineBackslash() throws {
        // Hard line breaks rendered with backslash
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_newline_backslash)");
    }

    func testOptionsNewlineSpaces() throws {
        // Hard line breaks rendered with trailing spaces
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_newline_spaces)");
    }

    func testOptionsOutputFormatDjot() throws {
        // Djot output format produces djot-compatible markup
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_output_format_djot)");
    }

    func testOptionsOutputFormatMarkdown() throws {
        // Default markdown output format produces standard markdown
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_output_format_markdown)");
    }

    func testOptionsOutputFormatPlain() throws {
        // Plain text output format strips markdown syntax
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_output_format_plain)");
    }

    func testOptionsPreprocessingAggressive() throws {
        // Aggressive preset removes nav, footer, aside unconditionally
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_preprocessing_aggressive)");
    }

    func testOptionsPreprocessingMinimal() throws {
        // Minimal preset preserves nav, footer, aside
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_preprocessing_minimal)");
    }

    func testOptionsPreprocessingRemoveForms() throws {
        // Forms are removed when remove_forms is true
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_preprocessing_remove_forms)");
    }

    func testOptionsPreserveTagsIframe() throws {
        // Iframe tags preserved as raw HTML in output
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_preserve_tags_iframe)");
    }

    func testOptionsSkipImagesTrue() throws {
        // Images are omitted from output when skipImages is true
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_skip_images_true)");
    }

    func testOptionsStripNewlines() throws {
        // Strip newlines produces single-line paragraphs
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_strip_newlines)");
    }

    func testOptionsStripTagsDivSpan() throws {
        // Div and span tags stripped but content preserved
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_strip_tags_div_span)");
    }

    func testOptionsStrongEmUnderscore() throws {
        // Strong and em tags use underscore symbol instead of asterisk
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_strong_em_underscore)");
    }

    func testOptionsSubSymbolTilde() throws {
        // Subscript rendered with tilde symbol
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_sub_symbol_tilde)");
    }

    func testOptionsSupSymbolCaret() throws {
        // Superscript rendered with caret symbol
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_sup_symbol_caret)");
    }

    func testOptionsWhitespaceNormalized() throws {
        // Normalized whitespace mode collapses multiple spaces
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_whitespace_normalized)");
    }

    func testOptionsWhitespaceStrict() throws {
        // Strict whitespace mode preserves whitespace as-is
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_whitespace_strict)");
    }

    func testOptionsWrapDisabled() throws {
        // Wrap option disabled preserves long lines without breaking
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_wrap_disabled)");
    }

    func testOptionsWrapEnabled() throws {
        // Wrap option enabled with custom width wraps long lines
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: options_wrap_enabled)");
    }

}
