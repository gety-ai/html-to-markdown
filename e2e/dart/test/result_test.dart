// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:030a6cbe1fdfaacb6602c0f223278c6738df15aa03718db788ba68cdb7f5b809
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'dart:io';
import 'package:h2m/h2m.dart';
import 'package:h2m/src/h2m_bridge_generated/frb_generated.dart' show RustLib;

// E2e tests for category: result
void main() {
  setUpAll(() async {
    await RustLib.init();
  });

  test('Result tables array is empty when input has no tables', () async {
    final result = await H2mBridge.convert('<p>No tables here</p>');
    expect(result.content, isNotNull);
    expect(result.tables?.length ?? 0, equals(0));
  });

  test('Multiple tables each appear in the tables array', () async {
    final result = await H2mBridge.convert('<table><tr><th>A</th></tr><tr><td>1</td></tr></table><p>Between</p><table><tr><th>B</th></tr><tr><td>2</td></tr></table>');
    expect(result.tables?.length ?? 0, greaterThanOrEqualTo(2));
  });

  test('Simple table populates the tables array in result', () async {
    final result = await H2mBridge.convert('<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>');
    expect(result.content, isNotNull);
    expect(result.tables?.length ?? 0, greaterThanOrEqualTo(1));
  });

  test('Tables array is empty when includeDocumentStructure is false', () async {
    final result = await H2mBridge.convert('<table><tr><th>X</th></tr><tr><td>Y</td></tr></table>');
    expect(result.content, isNotNull);
    expect(result.tables?.length ?? 0, equals(0));
  });

  test('Warnings array is empty for well-formed HTML without problematic content', () async {
    final result = await H2mBridge.convert('<h1>Title</h1><p>Clean content with <a href=\'https://example.com\'>a link</a>.</p>');
    expect(result.content, isNotNull);
    expect(result.warnings?.length ?? 0, equals(0));
  });

  test('Warnings array is empty for complex but valid HTML', () async {
    final result = await H2mBridge.convert('<article><h1>Article</h1><p>Paragraph with <strong>bold</strong> and <em>italic</em>.</p><table><tr><th>Col</th></tr><tr><td>Val</td></tr></table><ul><li>Item 1</li><li>Item 2</li></ul></article>');
    expect(result.content, isNotNull);
    expect(result.warnings?.length ?? 0, equals(0));
  });

  test('Warnings array is empty even for malformed HTML (parser is lenient)', () async {
    final result = await H2mBridge.convert('<p>Unclosed paragraph<div>Mixed nesting</p></div>');
    expect(result.content, isNotNull);
    expect(result.warnings?.length ?? 0, equals(0));
  });

}
