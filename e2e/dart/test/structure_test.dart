// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5360156d6ec3af437f3e2745880d956271026242dfb4cb9acf5fe48e8c0fa563
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'dart:io';
import 'package:h2m/h2m.dart';
import 'package:h2m/src/h2m_bridge_generated/frb_generated.dart' show RustLib;

// E2e tests for category: structure
void main() {
  setUpAll(() async {
    await RustLib.init();
  });

  test('Fenced code block produces Code node', () async {
    final result = await H2mBridge.convert('<p>Example code:</p><pre><code class="language-rust">fn main() { println!("Hello"); }</code></pre>');
    expect(result.content, isNotNull);
    expect(result.document?.nodes, isNotNull);
    expect(result.document?.nodes?.length ?? 0, greaterThanOrEqualTo(2));
  });

  test('H1 > H2 > H3 creates three levels of heading nesting', () async {
    final result = await H2mBridge.convert('<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>');
    expect(result.content, isNotNull);
    expect(result.document?.nodes, isNotNull);
    expect(result.document?.nodes?.length ?? 0, greaterThanOrEqualTo(5));
  });

  test('H1 followed by H2 creates a nested group under the H1', () async {
    final result = await H2mBridge.convert('<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>');
    expect(result.content, isNotNull);
    expect(result.document?.nodes, isNotNull);
    expect(result.document?.nodes?.length ?? 0, greaterThanOrEqualTo(3));
  });

  test('Simple heading followed by paragraph produces Heading and Paragraph nodes', () async {
    final result = await H2mBridge.convert('<h1>Title</h1><p>A paragraph of text.</p>');
    expect(result.content, isNotNull);
    expect(result.document?.nodes, isNotNull);
    expect(result.document?.nodes?.length ?? 0, greaterThanOrEqualTo(2));
  });

  test('Unordered list produces List and ListItem nodes', () async {
    final result = await H2mBridge.convert('<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>');
    expect(result.content, isNotNull);
    expect(result.document?.nodes, isNotNull);
    expect(result.document?.nodes?.length ?? 0, greaterThanOrEqualTo(2));
  });

  test('Multiple headings create multiple Heading nodes with correct levels', () async {
    final result = await H2mBridge.convert('<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>');
    expect(result.content, isNotNull);
    expect(result.document?.nodes, isNotNull);
    expect(result.document?.nodes?.length ?? 0, greaterThanOrEqualTo(4));
  });

  test('H1, H2, then another H1 creates two sibling top-level groups', () async {
    final result = await H2mBridge.convert('<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>');
    expect(result.content, isNotNull);
    expect(result.document?.nodes, isNotNull);
    expect(result.document?.nodes?.length ?? 0, greaterThanOrEqualTo(4));
  });

}
