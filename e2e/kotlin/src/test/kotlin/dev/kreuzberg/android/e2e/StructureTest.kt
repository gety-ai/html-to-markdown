// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8771997a6befb862cd9c5c8f5706d5080391524cb18c75879c3b6d3d349e1f7c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import dev.kreuzberg.android.HtmlToMarkdownAndroid
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import dev.kreuzberg.android.ConversionOptions

/** E2e tests for category: structure. */
class StructureTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testStructureCodeBlock() {
        // Fenced code block produces Code node
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<p>Example code:</p><pre><code class=\"language-rust\">fn main() { println!(\"Hello\"); }</code></pre>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes()!!.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testStructureDeepNestingH1H2H3() {
        // H1 > H2 > H3 creates three levels of heading nesting
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes()!!.size >= 5, "expected at least 5 elements")
    }

    @Test
    fun testStructureH1H2NestedGroup() {
        // H1 followed by H2 creates a nested group under the H1
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes()!!.size >= 3, "expected at least 3 elements")
    }

    @Test
    fun testStructureHeadingParagraph() {
        // Simple heading followed by paragraph produces Heading and Paragraph nodes
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<h1>Title</h1><p>A paragraph of text.</p>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes()!!.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testStructureList() {
        // Unordered list produces List and ListItem nodes
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes()!!.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testStructureMultipleHeadings() {
        // Multiple headings create multiple Heading nodes with correct levels
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes()!!.size >= 4, "expected at least 4 elements")
    }

    @Test
    fun testStructureSiblingH1Groups() {
        // H1, H2, then another H1 creates two sibling top-level groups
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes() != null, "expected non-empty value")
        assertTrue(result.document()?.nodes()!!.size >= 4, "expected at least 4 elements")
    }

}
