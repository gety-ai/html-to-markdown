// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e445c0c8d7a5e50464b2150ce36f69465942cfa5fcea8ebdb17aca5e2369dfbe
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import dev.kreuzberg.android.HtmlToMarkdownAndroid
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import dev.kreuzberg.android.ConversionOptions

/** E2e tests for category: result. */
class ResultTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testResultTablesEmptyWhenNoTables() {
        // Result tables array is empty when input has no tables
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<p>No tables here</p>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertEquals(0, result.tables().size, "expected exactly 0 elements")
    }

    @Test
    fun testResultTablesMultiple() {
        // Multiple tables each appear in the tables array
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<table><tr><th>A</th></tr><tr><td>1</td></tr></table><p>Between</p><table><tr><th>B</th></tr><tr><td>2</td></tr></table>", options)
        assertTrue(result.tables().size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testResultTablesSimple() {
        // Simple table populates the tables array in result
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = HtmlToMarkdownAndroid.convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", options)
        assertTrue(result.content() != null, "expected non-empty value")
        assertTrue(result.tables().size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testResultTablesWithoutStructureFlag() {
        // Tables array is empty when includeDocumentStructure is false
        val result = HtmlToMarkdownAndroid.convert("<table><tr><th>X</th></tr><tr><td>Y</td></tr></table>", ConversionOptions.builder().build())
        assertTrue(result.content() != null, "expected non-empty value")
        assertEquals(0, result.tables().size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForCleanInput() {
        // Warnings array is empty for well-formed HTML without problematic content
        val result = HtmlToMarkdownAndroid.convert("<h1>Title</h1><p>Clean content with <a href='https://example.com'>a link</a>.</p>", ConversionOptions.builder().build())
        assertTrue(result.content() != null, "expected non-empty value")
        assertEquals(0, result.warnings().size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForComplexInput() {
        // Warnings array is empty for complex but valid HTML
        val result = HtmlToMarkdownAndroid.convert("<article><h1>Article</h1><p>Paragraph with <strong>bold</strong> and <em>italic</em>.</p><table><tr><th>Col</th></tr><tr><td>Val</td></tr></table><ul><li>Item 1</li><li>Item 2</li></ul></article>", ConversionOptions.builder().build())
        assertTrue(result.content() != null, "expected non-empty value")
        assertEquals(0, result.warnings().size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForMalformedHtml() {
        // Warnings array is empty even for malformed HTML (parser is lenient)
        val result = HtmlToMarkdownAndroid.convert("<p>Unclosed paragraph<div>Mixed nesting</p></div>", ConversionOptions.builder().build())
        assertTrue(result.content() != null, "expected non-empty value")
        assertEquals(0, result.warnings().size, "expected exactly 0 elements")
    }

}
